def main(bool g1,bool g2,u8 a1,u8 a2,bool[20]  arr ,bool[20] arr2) -> bool {
    field mut count = 0;
    for u32 i in 2..20 {
        field temp = arr[i] == arr2[i] ? 1 : 0;
        count = count + temp;
    }
    return a1 - a2 > 5 || a2-a1 > 5 && g1 == g2 && count >= 16;
}